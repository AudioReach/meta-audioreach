# yaml-language-server: $schema=https://raw.githubusercontent.com/siemens/kas/master/kas/schema-kas.json
header:
  version: 14

distro: poky

defaults:
  repos:
    branch: scarthgap

repos:
  # BitBake 2.8 (external)
  bitbake:
    url: https://github.com/openembedded/bitbake.git
    rev: "2.8"
    layers:
      .: excluded

  # Poky (provides meta, meta-poky)
  poky:
    url: git://git.yoctoproject.org/poky
    layers:
      meta:
      meta-poky:

  # Raspberry Pi BSP layer
  meta-raspberrypi:
    url: git://git.yoctoproject.org/meta-raspberrypi

  # meta-openembedded (scarthgap)
  meta-openembedded:
    url: https://git.openembedded.org/meta-openembedded
    layers:
      meta-oe:
      meta-multimedia:
      meta-networking:
      meta-python:

  # AudioReach layer
  meta-audioreach:
    url: https://github.com/AudioReach/meta-audioreach.git
    layers:
      .:

local_conf_header:
  machine: |
    MACHINE = "raspberrypi4"

  are_support: |
    PACKAGECONFIG:pn-audioreach-graphmgr = "are_on_apps use_default_acdb_path"
    PACKAGECONFIG:pn-audioreach-graphservices = "are_on_apps"

  image_install: |
    IMAGE_INSTALL:append = " audioreach-graphservices tinyalsa audioreach-graphmgr audioreach-engine audioreach-conf"

  extra: |
    EXTRA_IMAGE_FEATURES = "allow-empty-password empty-root-password allow-root-login"

  license-accept: |
    LICENSE_FLAGS_ACCEPTED = "synaptics-killswitch"

machine: raspberrypi4
target: core-image-sato
build_system: oe