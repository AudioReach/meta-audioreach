---
name: Build
description: Build the project using a Docker container

inputs:
  docker_image:
    description: Docker image to use for the build job
    required: true

  kas_file:
    description: KAS file to use for the build job
    required: true

  build_command:
    description: Command to run for the build
    required: false
    default: "bitbake qcom-multimedia-image"

  build_type:
    description: Type of build to perform (flat_build or sdk_build)
    required: false
    default: "flat_build"

runs:
  using: "composite"
  steps:
    - name: Build ${{ env.MACHINE }}
      shell: bash
      run: |
        #!/bin/bash
        set -e

        # touch file_list.txt
        filepath="${{ github.workspace }}/${{ env.MACHINE }}_${{ inputs.build_type }}file_list.txt"
        echo "filepath=${filepath}" >> $GITHUB_ENV
        touch $filepath

        # Define variables
        USER_ID=$(id -u)
        GROUP_ID=$(id -g)
        USER=$(whoami)
        WORKSPACE=${{ github.workspace }}/..
        USER_HOME="/home/$USER"
        
        echo "::group::$(printf '__________ %-100s' 'build' | tr ' ' _)"
        docker run \
          --rm \
          --user $(id -u):$(id -g) \
          -v ${WORKSPACE}:${USER_HOME}/workspace \
          -w ${USER_HOME}/workspace \
          --privileged \
          --cpus=48 \
          --memory=96g \
          --ulimit nofile=1048576:1048576 \
          ${{ inputs.docker_image }} \
          bash -c '
            set -ex
            umask 022
            kas shell meta-audioreach/ci/${{ inputs.kas_file }} -c '
            cd meta-qcom
            git fetch https://github.com/qualcomm-linux/meta-qcom 0a330a3148346f902e3d9588407a585002b26a75 && git cherry-pick FETCH_HEAD
            cd -
            ${{ inputs.build_command }}
            '
            #if [ "${{ inputs.build_type }}" = "flat_build" ]; then
             # kas build meta-audioreach/ci/${{ inputs.kas_file }}
            #else
            # kas build meta-audioreach/ci/${{ inputs.kas_file }} -t populate_sdk
            #fi
          '
        echo "::endgroup::"

    - name: Create tar archive for the build
      if: ${{ inputs.build_type == 'flat_build' }}
      shell: bash
      run: |
        #!/bin/bash
        set -ex

        # Process the kas file to remove the .yml extension
        KAS_FILE="${{ inputs.kas_file }}"
        KAS_FILE="${KAS_FILE%.yml}"

        cd ..
        MACHINE=${{ env.MACHINE }}
        BUILD_DIR=build
        PUB_DIR=${{ github.workspace }}/pub/${MACHINE}/flat_build
        mkdir -p $PUB_DIR

        cd $BUILD_DIR/tmp/deploy/images/${KAS_FILE}
        cp qcom-multimedia-image-${KAS_FILE}.rootfs.qcomflash.tar.gz "$PUB_DIR/"
          
        # create file file_list
        echo "${PUB_DIR}/qcom-multimedia-image-${KAS_FILE}.rootfs.qcomflash.tar.gz" >> ${{ env.filepath }}
        ls -larth $PUB_DIR
        echo "Build artifacts created in $PUB_DIR"

    - name: copy the sdk to the pub directory
      if: ${{ inputs.build_type == 'sdk_build' }}
      shell: bash
      run: |
        #!/bin/bash
        set -ex

        cd ..
        cd build/tmp/deploy/sdk/
        sdk_file=$(ls qcom-distro-*.sh)
        PUB_DIR=${{ github.workspace }}/pub/${{ env.MACHINE }}/sdk_build
        mkdir -p $PUB_DIR
        cp $sdk_file $PUB_DIR/sdk-${MACHINE}.sh
        echo "${PUB_DIR}/sdk-${MACHINE}.sh" >> ${{ env.filepath }}
        ls -larth $PUB_DIR
        echo "SDK artifacts created in $PUB_DIR"

    - name: Upload artifacts
      uses: Audioreach/meta-audioreach/.github/actions/aws-s3-exchanger@ci_test
      with:
        mode: multi-upload
        local_file: ${{ env.filepath }}
        s3_bucket: qli-prd-audior-gh-artifacts
      env:
        MACHINE: ${{ env.MACHINE }}
        BUILD_TYPE: ${{ inputs.build_type }}

    - name: Update Summary
      shell: bash
      run: |
        echo "## Build Summary for ${{ env.MACHINE }}" >> $GITHUB_STEP_SUMMARY
        echo '### :rocket: Build Successful!' >> $GITHUB_STEP_SUMMARY
        echo "- Machine: ${{ env.MACHINE }}" >> $GITHUB_STEP_SUMMARY
        echo "- Config File: ${{ inputs.kas_file }}" >> $GITHUB_STEP_SUMMARY
        echo "- Build Type: ${{ inputs.build_type }}" >> $GITHUB_STEP_SUMMARY
        echo "- Build Command: ${{ inputs.build_command }}" >> $GITHUB_STEP_SUMMARY
