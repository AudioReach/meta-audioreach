name: Qualcomm Preflight Checks

on:
  pull_request_target:
    branches: [ ci_test ]
  push:
    branches: [ ci_test ]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  qcom-preflight-checks:
    uses: Audioreach/audioreach-workflows/.github/workflows/qcom-preflight-checks.yml@BB_test
    secrets: inherit
