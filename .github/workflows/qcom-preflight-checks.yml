name: Qualcomm Preflight Checks

on:
  pull_request:
    branches: [ upstream ]
  push:
    branches: [ upstream ]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  qcom-preflight-checks:
    uses: Audioreach/audioreach-workflows/.github/workflows/qcom-preflight-checks.yml@upstream
    secrets: inherit
